#include <stdio.h>

int main()

{
    int n, a, b;

    scanf("%d", &n);

    for(a = 1; a <= n; a++)
    {

        b = 1;
        while(b <= n)
        {
            printf("%d", n);
            b++;
        }

        printf("\n");
    }

    return 0;

}
