#include <bits/stdc++.h>

using namespace std; 

int main() 
{
    string a1;
    getline(cin, a1);
    for (int i = 0; a1[i] != '\0'; i++) 
    {
        if (a1[i] != '+') 
        {
            sort(a1.begin(), a1.end())
        }
    }

    cout << a1;
}